import Store from "@commonjs/Store.js";
export const { mapActions, mapGetters, mapMutations, mapState } = new Store({
  namespaced: true,
  state: {},
  mutations: {},
  getters: {},
  actions: {},
});
